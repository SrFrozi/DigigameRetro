

function promptart() {
    while (true) {
        var caminho = prompt('1- Atacar com a espada sagrada 2- Atacar com o canhão de prótons  3- Atacar com a investida X ');

        if (caminho == 1) {
            return location = ('../ganhoumane.html');
        }
        else if (caminho == 2) {
            return location = ('../ganhoumane.html');
        }
        else if (caminho == 3) {
            return location = ('../ganhoumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function perdeu() {
    while (true) {
        var caminho = prompt('Digite 1 para retornar ao menu inicial.');
        if (caminho == 1) {
            return location = ('digigameretro.html')
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function selecionart() {
    while (true) {
        var caminho = prompt('1- Atacar com a espada sagrada 2- Atacar com o canhão de prótons  3- Atacar com a investida X ');

        if (caminho == 1) {
            return location = ('../ganhoumane.html');
        }
        else if (caminho == 2) {
            return location = ('../ganhoumane.html');
        }
        else if (caminho == 3) {
            return location = ('../ganhoumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}