function promptar() {
    while (true) {
        var caminho = prompt('1- Abandonar Agumon 2- Lutar junto a Agumon 3- Tentar Digievolução');

        if (caminho == 1) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 2) {
            return location = ('./parte3.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function promptart() {
    while (true) {
        var caminho = prompt('1- Fugir com Greymon 2- Tentar lutar com Infermon 3- Forçar Digievolução');

        if (caminho == 1) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 2) {
            return location = ('./parte3t.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function selecionar() {
    while (true) {
        var caminho = prompt('1- Atacar Infermon. 2- Pegar Gabumon e fugir 3- Forçar Digievolução');

        if (caminho == 1) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 2) {
            return location = ('./parte3.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function selecionart() {
    while (true) {
        var caminho = prompt('1- Fugir com Garurumon 2- Tentar lutar com Infermon 3- Forçar Digievolução');

        if (caminho == 1) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 2) {
            return location = ('./parte3t.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function escolher() {
    while (true) {
        var caminho = prompt('1- Lutar com Infermon. 2- Recuar de Infermon 3- Forçar Digievolução');

        if (caminho == 1) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 2) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 3) {
            return location = ('./parte3.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}


function perdeu() {
    while (true) {
        var caminho = prompt('Digite 1 para retornar ao menu inicial.');
        if (caminho == 1) {
            return location = ('digigameretro.html')
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}