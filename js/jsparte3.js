function promptar() {
    while (true) {
        var caminho = prompt('1- Continuar lutando 2- Tentar digievolução 3- Fugir de Armageddemon');

        if (caminho == 1) {
            return location = ('./parte4.html');
        }
        else if (caminho == 2) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function promptart() {
    while (true) {
        var caminho = prompt('1- chamar ajuda no digivice 2- tentar evolução final 3- ir pra cima de Diaboromon');

        if (caminho == 1) {
            return location = ('./parte4t.html');
        }
        else if (caminho == 2) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function selecionar() {
    while (true) {
        var caminho = prompt('1- Tentar Digievoluir 2- Deixar Gabumon pra trás 3- Se sacrificar pelo seu Digimon.');

        if (caminho == 1) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 2) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 3) {
            return location = ('./parte4.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function escolher() {
    while (true) {
        var caminho = prompt('1- Tentar Digievoluir 2- Atacar Infermon com tudo 3- ficar na defensiva');

        if (caminho == 1) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 2) {
            return location = ('../ganhoumane.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function selecionart() {
    while (true) {
        var caminho = prompt('1- Atacar Diaboromon 2-Forçar digievolução  3- Pedir ajuda no Digivice!');

        if (caminho == 1) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 2) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 3) {
            return location = ('./parte4t.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function perdeu() {
    while (true) {
        var caminho = prompt('Digite 1 para retornar ao menu inicial.');
        if (caminho == 1) {
            return location = ('digigameretro.html')
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}
