function promptar() {
    while (true) {
        var caminho = prompt('1- Treinar. 2- Ensinar valores. 3- Diversão!');

        if (caminho == 1) {
            return location = ('./parte2t.html');
        }
        else if (caminho == 2) {
            return location = ('./parte2.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function selecionar() {
    while (true) {
        var caminho = prompt('1- Treinar. 2- Ensinar valores. 3- Diversão!');

        if (caminho == 1) {
            return location = ('./parte2t.html');
        }
        else if (caminho == 2) {
            return location = ('./parte2.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function escolher() {
    while (true) {
        var caminho = prompt('1- Treinar. 2- Ensinar valores. 3- Diversão!');

        if (caminho == 1) {
            return location = ('./parte2.html');
        }
        else if (caminho == 2) {
            return location = ('../perdeumane.html');
        }
        else if (caminho == 3) {
            return location = ('../perdeumane.html');
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}

function perdeu() {
    while (true) {
        var caminho = prompt('Digite 1 para retornar ao menu inicial.');
        if (caminho == 1) {
            return location = ('digigameretro.html')
        }
        else {
            alert('Por favor, selecione uma opção válida.');
        }

    }
}